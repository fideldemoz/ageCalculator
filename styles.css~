:root {
	--purple: hsl(259, 100%, 65%);
	--light-red: hsl(0, 100%, 67%);
	--white: hsl(0, 0%, 100%);
	--off-white: hsl(0, 0%, 94%);
	--light-gray: hsl(0, 0%, 86%);
	--smokey-grey: hsl(0, 1%, 44%);
	--off-black: hsl(0, 0%, 8%);
}
@font-face {
    font-family: 'PoppinsBoldItalic';
    src: url('./assets/fonts/Poppins-BoldItalic.ttf');
    font-weight: bold;
    font-style: italic;
    }

@font-face {
    font-family: 'PoppinsBold';
    src: url('./assets/fonts/Poppins-Bold.ttf');
    font-weight: bold;
    font-style: normal;
}
@font-face {
    font-family: 'PoppinsRegular';
    src: url('./assets/fonts/Poppins-Regular.ttf');
    font-weight: 400;
    font-style: normal;
}
* {
	padding: 0;
	margin: 0;
}
body {
	display: grid;
	height: 100vh;
	place-items: center;
	font-size: 1.4rem;
	background-color: var(--light-gray);
}
main {
	background-color: var(--white);
	padding: 1rem;
	border-radius: 8px 8px 100px 8px;
	font-family: 'PoppinsRegular';
}
.title {
	text-align: center;
	margin-bottom: 1rem;
}
input, select {
	border: none;
	outline: none;
	font-size: 32px;
	padding: .4rem;
	font-weight: 900;
	margin: 4px;
	border: 1px solid var(--smokey-grey);
	border-radius: 8px;
}
input:focus {
	border: 1px solid var(--purple);
}
input[type="submit"] {
	background-color: green;
	position: relative;
	left: 	86%;
	top: .4rem;
	width: 60px;
	height: 60px;
	border-radius: 100px;
	background: var(--purple) url(assets/images/icon-arrow.svg);
	background-size: cover;
	border:0;
}
input[type="submit"]:hover {
	background-color: var(--off-black);
}
form {
	color: var(--smokey-grey);
}
form div{
	display: grid;
	grid-template: 21px 1fr/ 120px 200px 120px;
}
#date {
	grid-area: 2/1/3/2; 
}
select {
	grid-area: 2/2/3/3; 	
}
h2 span {
	color: var(--purple);
}
hr {
	margin-bottom: 2rem;
  margin-top: -2rem;
}
div h2 {
	font-family: 'PoppinsBoldItalic';
}

@media screen and (max-width: 720px) {
	input[type="submit"] {
		left: 50%;
	}
	main {
		margin: 2px;
	}
}